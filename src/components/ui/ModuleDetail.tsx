import { observer } from "mobx-react-lite";
import react from "react"
import "../../Style/app.css"

const ModuleDetailUI = () => {
    return (
        <section>
            <div className="center">
                test
            </div>
        </section>
    )
}

export default observer(ModuleDetailUI);